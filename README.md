# Quantum-Safe-Email-
Desktop email client plugin that encrypts outgoing emails using hybrid encryption (RSA + MLKEM), signs with ML-DSA, stores encrypted copies locally, decrypts incoming emails, and maintains local key directory without cloud sync.
